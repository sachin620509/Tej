import { Redirect } from 'expo-router';import { ActivityIndicator,View } from 'react-native';import { useMobileAuth } from '../src/auth';export default function Index(){const {user,loading}=useMobileAuth();if(loading)return <View style={{flex:1,alignItems:'center',justifyContent:'center'}}><ActivityIndicator color="#245d4e"/></View>;return <Redirect href={user?'/(tabs)':'/login'}/>}
