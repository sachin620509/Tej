import * as SecureStore from 'expo-secure-store';import type {CameraDraft,CapturedMedia} from './types';const KEY='instaframe.cameraDrafts.v1',MAX=12;
async function list():Promise<CameraDraft[]>{try{return JSON.parse(await SecureStore.getItemAsync(KEY)??'[]') as CameraDraft[]}catch{return []}}
export const DraftService={list,async save(media:CapturedMedia,title='Untitled draft'){const drafts=await list(),draft={id:`draft-${Date.now()}`,title,media,updatedAt:new Date().toISOString()};await SecureStore.setItemAsync(KEY,JSON.stringify([draft,...drafts.filter(x=>x.media.uri!==media.uri)].slice(0,MAX)));return draft},async remove(id:string){await SecureStore.setItemAsync(KEY,JSON.stringify((await list()).filter(x=>x.id!==id)))},async clear(){await SecureStore.deleteItemAsync(KEY)}};
