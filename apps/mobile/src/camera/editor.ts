import type {CapturedMedia,EditorOverlay,MediaEdit} from './types';
export const defaultEdit=():MediaEdit=>({rotation:0,filter:'none',speed:1,overlays:[]});
export const MediaEditorService={rotate(media:CapturedMedia):CapturedMedia{return {...media,edit:{...media.edit,rotation:((media.edit.rotation+90)%360) as MediaEdit['rotation']}}},filter(media:CapturedMedia,filter:MediaEdit['filter']):CapturedMedia{return {...media,edit:{...media.edit,filter}}},trim(media:CapturedMedia,startMs:number,endMs:number):CapturedMedia{const duration=media.durationMs??endMs;if(startMs<0||endMs<=startMs||endMs>duration)throw new Error('Invalid trim range');return {...media,edit:{...media.edit,trim:{startMs,endMs}}}},speed(media:CapturedMedia,speed:MediaEdit['speed']):CapturedMedia{return {...media,edit:{...media.edit,speed}}},overlay(media:CapturedMedia,input:Omit<EditorOverlay,'id'>):CapturedMedia{return {...media,edit:{...media.edit,overlays:[...media.edit.overlays,{...input,id:`overlay-${Date.now()}`}]}}},removeOverlay(media:CapturedMedia,id:string):CapturedMedia{return {...media,edit:{...media.edit,overlays:media.edit.overlays.filter(x=>x.id!==id)}}},audio(media:CapturedMedia,uri:string,licensedByUser:boolean):CapturedMedia{if(!licensedByUser)throw new Error('Only user-owned or properly licensed audio is allowed');return {...media,edit:{...media.edit,audio:{uri,licensedByUser:true,volume:1}}}}};
export const ExportService={manifest(media:CapturedMedia){return {sourceUri:media.uri,kind:media.kind,mode:media.mode,edit:media.edit,requestedAt:new Date().toISOString()}}};
