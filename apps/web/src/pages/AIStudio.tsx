import { useState } from 'react';import { AIAssistant } from '../components/AIAssistant';export function AIStudio(){const [draft,setDraft]=useState('');return <div className="page ai-studio"><p className="eyebrow">PRIVACY-FIRST CREATOR TOOLS</p><h1>AI writing studio</h1><p>Draft captions, bios, alt text, translations and summaries. Nothing is auto-published or silently processed.</p><textarea value={draft} onChange={event=>setDraft(event.target.value)} maxLength={4000} placeholder="Write a topic or draft you explicitly want help with…"/><AIAssistant text={draft} onChoose={value=>setDraft(current=>value.startsWith('#')?`${current} ${value}`.trim():value)}/><small>Review every suggestion before copying it into a post, reel, story, profile or creator description.</small></div>}
