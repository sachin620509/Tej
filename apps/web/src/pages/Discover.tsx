import { DISCOVERY_DISCLAIMER } from '@instaframe/contracts';import { Camera,CheckCircle2,ShieldCheck,Sparkles,Upload } from 'lucide-react';import { Link } from 'react-router-dom';
export function Discover(){return <div className="page discover"><div className="discovery-hero"><span className="glow-icon"><Sparkles/></span><p className="eyebrow">Consent-first discovery</p><h1>Find an InstaFrame profile</h1><p>Search among members who chose to be discoverable. Nothing outside InstaFrame is searched or scraped.</p></div><div className="discovery-grid"><section className="upload-card"><div className="dropzone"><Camera/><b>Open InstaFrame Scan</b><span>Use your camera or choose JPEG, PNG, or WebP</span><Link className="primary" to="/scan"><Upload size={17}/> Start Scanner</Link></div><div className="notice"><ShieldCheck/><p><b>Your search stays limited.</b><br/>{DISCOVERY_DISCLAIMER}</p></div></section><aside className="trust-card"><h2>Built around permission</h2>{['Only approved profile photos are indexed','Members can leave the index at any time','Private details and internal match data stay hidden','Search activity is rate-limited and audited'].map(text=><p key={text}><CheckCircle2/>{text}</p>)}<small>This feature does not establish real-world identity and should not be used for identity verification.</small></aside></div></div>}
