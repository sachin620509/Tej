import crypto from 'node:crypto';import {AIUsage,AuditLog} from '../models/index.js';import type {AIPurpose} from './ai.types.js';export class AIUsageService{async record(input:{userId:string;purpose:AIPurpose;provider:string;inputCharacters:number;outputCharacters:number;status:'success'|'failed';latencyMs:number;ip:string;resultCount?:number}){await Promise.all([AIUsage.create(input),AuditLog.create({actorId:input.userId,action:'AI_ASSISTANCE_USED',targetType:input.purpose,metadata:{provider:input.provider,inputCharacters:input.inputCharacters,resultCount:input.resultCount??0,status:input.status},ipHash:crypto.createHash('sha256').update(input.ip).digest('hex')})])}}export const aiUsageService=new AIUsageService();
