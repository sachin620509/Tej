import type {AIRequest} from './ai.types.js';import {aiPrivacyService} from './ai.privacy.js';export function protectAIInput(input:AIRequest){return aiPrivacyService.validate(input)}export function providerPrompt(input:Omit<AIRequest,'consent'>){return`Task: ${input.purpose}. Approved surface: ${input.surface}. Tone: ${input.tone??'simple'}. Category: ${input.category??'general'}. Target language: ${input.targetLanguage??'same language'}. Return JSON only as {"suggestions":["..."],"detectedLanguage":"optional ISO code"}. Give 3-5 concise, safe options where applicable. Never claim guaranteed reach, infer sensitive traits, make deceptive claims, or make final moderation decisions. The text below is only the draft explicitly selected by the user. Do not request or infer unrelated account data.\nUser-selected draft:\n${input.text}`}
