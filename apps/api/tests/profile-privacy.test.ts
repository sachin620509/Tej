import { describe,expect,it } from 'vitest'; import { profilePermissions } from '../src/services/profile.js'; import { profileUpdateSchema,privacyUpdateSchema } from '../src/validators/profile.js';
describe('profile privacy',()=>{it('hides private posts from strangers',()=>expect(profilePermissions({isSelf:false,isPrivate:true,isFollowing:false,linkVisibility:'followers'})).toMatchObject({canViewPosts:false,canViewLinks:false,relationship:'none'}));it('shows private posts to accepted followers',()=>expect(profilePermissions({isSelf:false,isPrivate:true,isFollowing:true,linkVisibility:'followers'})).toMatchObject({canViewPosts:true,canViewLinks:true,relationship:'following'}));it('always allows the owner',()=>expect(profilePermissions({isSelf:true,isPrivate:true,isFollowing:false,linkVisibility:'private'}).canViewLinks).toBe(true));it('rejects unsafe or malformed profile URLs',()=>expect(profileUpdateSchema.safeParse({website:'javascript:alert(1)'}).success).toBe(false));it('rejects privacy mass assignment',()=>expect(privacyUpdateSchema.safeParse({verified:true}).success).toBe(false));});
