import { describe,expect,it } from 'vitest';import { diversifyByCreator,escapeRegex } from '../src/services/ranking.js';describe('safe explore ranking',()=>{it('caps repeated creators',()=>{const items=[{id:1,c:'a'},{id:2,c:'a'},{id:3,c:'a'},{id:4,c:'b'}];expect(diversifyByCreator(items,x=>x.c,4,2).map(x=>x.id)).toEqual([1,2,4])});it('escapes regular expression controls',()=>expect(escapeRegex('a.*(b)')).toBe('a\\.\\*\\(b\\)'));});
