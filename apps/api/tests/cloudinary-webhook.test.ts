import { describe,expect,it } from 'vitest';import { createCloudinaryNotificationSignature,verifyCloudinaryNotification } from '../src/services/cloudinaryWebhook.js';
describe('Cloudinary webhook signatures',()=>{const secret='a-secure-cloudinary-test-secret',body=Buffer.from('{"notification_type":"upload","public_id":"instaframe/user/posts/photo"}'),now=1800000000;it('accepts an authentic fresh raw payload',()=>{const signature=createCloudinaryNotificationSignature(body,now,secret);expect(verifyCloudinaryNotification({body,timestamp:String(now),signature,secret,nowSeconds:now})).toBe(true)});it('rejects body tampering and malformed signatures',()=>{const signature=createCloudinaryNotificationSignature(body,now,secret);expect(verifyCloudinaryNotification({body:Buffer.from(`${body}x`),timestamp:String(now),signature,secret,nowSeconds:now})).toBe(false);expect(verifyCloudinaryNotification({body,timestamp:String(now),signature:'bad',secret,nowSeconds:now})).toBe(false)});it('rejects stale replay attempts',()=>{const signature=createCloudinaryNotificationSignature(body,now,secret);expect(verifyCloudinaryNotification({body,timestamp:String(now),signature,secret,nowSeconds:now+7201})).toBe(false)})});
